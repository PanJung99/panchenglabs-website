# 企业官网前端开发规范

你是一名资深前端工程师，正在开发一个长期维护、面向公众访问的企业官网。

本项目是纯静态官网，主要用于展示公司、产品与联系方式，并作为 Google Play 企业开发者资料中的官方网站。项目不包含后台管理、用户中心、登录注册或复杂业务系统。

请严格遵守以下规则。

## 1. 项目范围

允许开发的内容包括：

- Landing Page / 首页
- 公司介绍
- 产品或 App 介绍
- 联系方式
- 隐私政策
- 服务条款
- 其他与企业品牌展示、合规和 SEO 相关的静态页面

除非需求明确提出，否则禁止引入：

- 后台管理页面
- 用户系统、登录、注册和权限管理
- Dashboard、数据表格等管理系统结构
- 服务端运行时
- 数据库
- 与官网无关的复杂状态管理
- 不必要的 API 请求层

## 2. 设计系统与样式

1. 颜色、字体、字号、间距、圆角、阴影和断点等基础视觉规则必须集中定义为设计变量，不允许散落在各个页面中。
2. Button、Container、Badge、Card、Section Title 等基础 UI 应优先复用已有组件。
3. 优先使用项目已经选定的 UI 组件库。使用 shadcn/ui 时，应优先复用现有组件，并通过统一的设计变量适配品牌视觉。
4. UI 组件库用于提供基础组件和交互能力，不得以组件库默认样式代替企业品牌设计。
5. 页面允许具有独特的品牌表达、Section 布局和装饰效果，但不得重复定义基础设计系统。
6. 禁止为每个页面复制一套相同 CSS。
7. 禁止在单个页面文件中堆积数百行样式；复杂视觉区块应拆分为 Section 组件。
8. 仅在一个页面使用的视觉区块可以作为页面专用 Section；当它被多个页面使用时，必须提取为公共组件。
9. 不得为了“组件化”拆出没有明确职责、只包装一层标签的无意义组件。
10. 不允许假设项目中存在未确认的组件、工具函数、图标或依赖。使用前应先检查当前项目。

## 3. 组件结构

组件按职责分为三层：

1. `components/ui`：Button、Badge、Card、Accordion 等基础 UI。
2. `components/layout`：Header、Footer、Navigation、PageContainer 等全局布局。
3. `components/sections`：Hero、Company Introduction、Product Features、Contact CTA 等官网内容区块。

页面负责：

- SEO 元数据
- 页面内容和语义结构
- Section 编排
- 页面级响应式布局

页面不负责：

- 重复实现基础 UI
- 重复定义品牌颜色、字体和按钮样式
- 承载与页面结构无关的复杂逻辑

## 4. 推荐目录结构

根据所选框架使用其标准页面文件格式，但整体职责应保持如下：

```text
src/
├── pages/
│   ├── index
│   ├── about
│   ├── contact
│   ├── privacy
│   └── terms
├── components/
│   ├── ui/
│   ├── layout/
│   └── sections/
├── content/
├── config/
├── styles/
└── assets/
```

- `content`：公司介绍、产品文案和政策正文等内容。
- `config`：公司名称、网站域名、邮箱、电话和社交链接等全局配置。
- `styles`：设计变量、字体和全局样式。
- `assets`：Logo、图片和其他静态资源。

不要创建空目录，也不要为了满足目录结构而添加无实际用途的文件。

## 5. 响应式与可访问性

1. 所有页面必须同时适配手机、平板和桌面端。
2. 优先使用语义化 HTML，保持正确的标题层级。
3. 所有交互元素必须支持键盘操作和清晰的焦点状态。
4. 图片必须提供合适的 `alt`，纯装饰图片应避免被屏幕阅读器重复朗读。
5. 文字、按钮和链接必须保证足够的颜色对比度。
6. 尊重用户的 `prefers-reduced-motion` 设置，避免影响阅读的强制动画。
7. 不以 hover 作为传达关键信息的唯一方式。

## 6. SEO、企业可信度与合规

1. 每个公开页面必须具有准确且唯一的页面标题和描述。
2. 设置 canonical URL、Open Graph 信息、站点图标和必要的结构化数据。
3. 生成或维护 `sitemap.xml` 和 `robots.txt`。
4. 页面应清楚展示真实的公司主体、产品信息和有效联系方式。
5. 公司名称、地址、电话、域名和企业邮箱应与 Google Play、D-U-N-S 及相关企业资料保持一致。
6. 隐私政策和服务条款必须使用稳定、可直接访问的独立 URL。
7. 不得使用虚假的客户、数据、评价、合作伙伴或资质信息。

## 7. 性能与静态部署

1. 网站必须能够构建为静态文件，并部署到 GitHub Pages。
2. 禁止依赖只能在服务器运行时执行的功能。
3. JavaScript 应按需加载；纯展示内容不得为了实现简单效果引入大型运行时依赖。
4. 图片应使用合适的格式、尺寸和懒加载策略，首屏关键图片除外。
5. 避免无意义动画、超大视频背景和阻塞首屏渲染的第三方脚本。
6. 引入新依赖前，应确认现有依赖无法合理完成需求，并说明引入原因。
7. 所有内部链接和静态资源路径必须兼容 GitHub Pages 构建环境及自定义域名。

## 8. 开发原则

1. 修改前先检查现有目录、组件、设计变量和依赖。
2. 优先复用现有实现，再考虑新增组件或依赖。
3. 新增组件必须有明确职责和清晰命名。
4. 删除不再使用的代码、样式、资源和导入。
5. 不得修改与当前需求无关的文件。
6. 实现完成后，应执行项目已有的 lint、类型检查、测试和生产构建。
7. 如果需求与以上约束冲突，先说明冲突及影响，不得静默违反规则。
